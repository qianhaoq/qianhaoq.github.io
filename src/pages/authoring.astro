---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="写作指南"
  description="Hao Qian 博客的作者写作指南：公开站点提供入口，真实编辑仍在本地仓库和 GitHub 工作流内完成。"
>
  <section class="page-hero">
    <p class="eyebrow">Authoring</p>
    <h1>写作指南</h1>
    <p>
      这个页面是公开站点里的作者入口说明，不是在线后台。文章创建、预览、验证和发布仍在本地仓库命令与 GitHub 权限边界内完成。
    </p>
  </section>

  <section class="section-band">
    <div class="authoring-flow" aria-label="作者写作流程">
      <article class="workflow-card">
        <span class="step-index">01</span>
        <h2>进入本地作者入口</h2>
        <p>在仓库根目录查看写作命令、草稿约束和发布路径。</p>
        <code>pnpm author</code>
      </article>
      <article class="workflow-card">
        <span class="step-index">02</span>
        <h2>创建私有草稿</h2>
        <p>新文章默认写入 `src/content/posts/`，并保持 `draft: true`。</p>
        <code>pnpm write "文章标题"</code>
      </article>
      <article class="workflow-card">
        <span class="step-index">03</span>
        <h2>本地预览与检查</h2>
        <p>先本地预览，再用完整门禁覆盖构建、单测、BDD 和静态产物。</p>
        <code>pnpm dev</code>
      </article>
      <article class="workflow-card">
        <span class="step-index">04</span>
        <h2>通过 PR 发布</h2>
        <p>发布前取消草稿状态，PR 通过质量门禁、AI review 和真实部署验证后合并。</p>
        <code>pnpm quality</code>
      </article>
    </div>
  </section>

  <section class="section-band muted">
    <div class="split-section">
      <div>
        <p class="eyebrow">Boundary</p>
        <h2>公开入口只做导航，不做写入</h2>
        <p>
          GitHub Pages 运行时是纯静态站点，所以这里不会提供登录、token 保存、在线编辑、发布按钮或服务端写仓库能力。
          需要修改内容时，请回到本地仓库或 GitHub PR 工作流。
        </p>
        <p>
          更完整的本地说明见仓库里的 <a class="inline-link" href="https://github.com/qianhaoq/qianhaoq.github.io/blob/main/AUTHORING.md">AUTHORING.md</a>。
        </p>
      </div>
      <div class="guardrail-list" aria-label="作者入口边界">
        <span>无公开 /admin</span>
        <span>无登录态</span>
        <span>无 GitHub token</span>
        <span>无服务端写入</span>
        <span>草稿默认私有</span>
        <span>PR 门禁发布</span>
      </div>
    </div>
  </section>
</BaseLayout>
